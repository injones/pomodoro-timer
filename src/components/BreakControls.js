import React from 'react';

/**
 * Stateless Component for controls to set up breaking functionality. Optional functionality so is kept separate from
 * TimerControls
 * @param props
 * @returns {*}
 * @constructor
 */
const BreakControls = (props) => {
    return (
        <div className='break-controls'>

        </div>
    )
};